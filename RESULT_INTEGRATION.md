# Reference result integration contract

Accept only a compact v2r_reference bundle with SEALED.json, SHA256SUMS, reference_recipe.json, run_manifest.json and scientific_provenance.json. Verify every hash, execution SHA, model/tokenizer/dataset revisions, reference gates and metric denominator before importing any row. Stage-specific components may be explicitly inapplicable; unavailable optional Dream/mechanism/localization evidence cannot block independent LLaDA core/temporal sealing and cannot be represented as zero.

Table1: reference trajectory accuracy, sampled failed trajectories probed, confirmed native/canonical recovery, never-correct recovery and temporal-subset T_last. Table2: paired LLaDA mechanisms with successful harm and NFE. Table3: prospective frozen OOF selector, accuracy delta including success harm, coverage/oracle gap/NFE. Figure2: temporal qC/qR/Delta and survival from independently confirmed dense subset, no monotonicity assumption.

Import actual numbers only after the controller's seal verifier approves the artifact. Current table files intentionally contain dashes. No importer implementation or numerical import is claimed complete yet. Existing V2 artifacts remain in a separate measurement-decoder appendix comparison and cannot populate reference missing cells.
